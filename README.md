# PyTorch + Transformer 学习笔记

这是 Ray Wang 的学习笔记仓库，记录了使用 PyTorch 和 Transformer 构建 AI 模型的过程。

## 内容结构
- `docs/`：Markdown 格式的理论笔记
- `notebooks/`：Jupyter Notebook 示例
- `scripts/`：Python 脚本

## 环境依赖
请使用以下命令安装所需依赖：
```bash
pip install -r requirements.txt
```

## 推荐工具
- VS Code + Remote SSH
- Jupyter Notebook
- GitHub Pages（可选）
